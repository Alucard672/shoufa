# 数据库集合和索引创建清单

## 云环境信息
- **环境ID**: `cloud1-1gzk1uq14c3065cb`
- **数据库类型**: 微信云开发数据库（NoSQL）

---

## 🔐 数据库权限设置说明

### 权限类型说明

微信云开发数据库支持以下权限类型：

1. **仅创建者可读写**
   - 仅创建者可以读写数据
   - 其他用户无法访问
   - **适用于**: 敏感数据、用户私有数据

2. **所有用户可读，仅创建者可写**
   - 所有用户可读取数据
   - 仅创建者可以写入
   - **适用于**: 字典数据、配置数据（前端需要读取但不应修改）

3. **所有用户可读**
   - 所有用户可读取数据
   - 所有用户可写入数据（不推荐用于生产环境）
   - **适用于**: 公开数据（生产环境不建议使用）

4. **所有用户不可读写**
   - 所有用户都无法直接访问
   - 仅云函数可以访问
   - **适用于**: 高度敏感数据，必须通过云函数统一管理

5. **自定义权限**
   - 可以编写安全规则，精确控制访问权限
   - 支持基于字段值的条件判断
   - **适用于**: 需要复杂权限控制的数据

### 权限设置建议

**重要说明**: 
- 本项目采用多租户架构，所有数据操作都应通过云函数进行
- 云函数具有管理员权限，可以访问所有数据
- 前端直接访问数据库的场景较少，建议大部分集合设置为限制性权限

**推荐配置**:

| 集合名称 | 推荐权限 | 说明 |
|---------|---------|------|
| `tenants` | 仅创建者可读写 或 所有用户不可读写 | 租户信息敏感，应仅通过云函数访问 |
| `users` | 所有用户不可读写 | 用户信息包含敏感数据（手机号、openid），必须仅通过云函数访问 |
| `styles` | 仅创建者可读写 | 款号数据包含业务信息，应通过云函数统一管理 |
| `factories` | 仅创建者可读写 | 加工厂信息应通过云函数统一管理 |
| `yarn_inventory` | 仅创建者可读写 | 库存数据敏感，应通过云函数统一管理 |
| `issue_orders` | 仅创建者可读写 | 发料单数据包含业务核心信息，应通过云函数统一管理 |
| `return_orders` | 仅创建者可读写 | 回货单数据包含业务核心信息，应通过云函数统一管理 |
| `color_dict` | 所有用户可读，仅创建者可写 | 字典数据前端可能需要直接读取（用于下拉选择等） |
| `size_dict` | 所有用户可读，仅创建者可写 | 字典数据前端可能需要直接读取（用于下拉选择等） |

### 权限设置步骤

1. 打开微信开发者工具
2. 点击顶部菜单栏的"云开发"
3. 进入"数据库"标签页
4. 选择对应的集合（如 `tenants`）
5. 点击"权限设置"标签
6. 选择推荐的权限类型
7. 点击"确定"保存

**注意**: 
- 如果前端代码中需要直接读取某些数据（如字典表），请相应调整权限设置
- 如果所有数据访问都通过云函数，建议设置为"仅创建者可读写"或"所有用户不可读写"

---

## 📋 需要创建的数据库集合（Collections）

### 1. tenants（租户表）
**用途**: 存储企业/租户信息

**权限设置**: 
- **推荐**: `仅创建者可读写` 或 `所有用户不可读写`
- **说明**: 租户信息敏感，应仅通过云函数访问，前端不应直接操作

**字段结构**:
- `sn` (String): 企业编号（唯一）
- `code` (String): 企业代码
- `crmTenantId` (String): CRM租户ID
- `name` (String): 租户名称
- `phone` (String): 联系电话
- `expireDate` (Date): 过期日期
- `loginLimitNum` (Number): 登录限制数量
- `demoFlag` (Boolean): 是否演示企业
- `stopFlag` (Boolean): 是否停用
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间

**必要索引**:
- `idx_sn`: `sn` (唯一索引) - 用于根据企业编号查询

---

### 2. users（用户表）
**用途**: 存储用户信息，关联租户和手机号

**权限设置**: 
- **推荐**: `所有用户不可读写`
- **说明**: 用户信息包含敏感数据（手机号、openid），必须仅通过云函数访问，前端不应直接操作

**字段结构**:
- `tenantId` (String): 租户ID
- `openid` (String): 微信OpenID（唯一）
- `phone` (String): 手机号
- `nickName` (String): 昵称
- `avatarUrl` (String): 头像URL
- `role` (String): 角色（admin/staff）
- `lastLoginTime` (Date): 最后登录时间
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记（默认 false）

**必要索引**:
- `idx_phone`: `phone` (普通索引) - 用于登录时根据手机号查询用户
- `idx_tenantId`: `tenantId` (普通索引) - 用于查询租户下的所有用户
- `idx_deleted`: `deleted` (普通索引) - 用于过滤已删除的用户记录
- `idx_deleted_tenantId`: `deleted` (asc) + `tenantId` (asc) (复合索引) - 用于查询租户下未删除的用户列表

---

### 3. styles（款号表）
**用途**: 存储款号信息

**权限设置**: 
- **推荐**: `仅创建者可读写`
- **说明**: 款号数据包含业务信息，应通过云函数统一管理，前端通过云函数接口访问

**字段结构**:
- `tenantId` (String): 租户ID
- `styleCode` (String): 款号编号
- `styleName` (String): 款号名称
- `imageUrl` (String): 款式图片URL
- `category` (String): 类别
- `yarnUsagePerPiece` (Number): 单件纱线用量（克）
- `lossRate` (Number): 损耗率（%）
- `actualUsage` (Number): 实际用量（含损耗，kg）
- `availableColors` (Array): 可选颜色列表
- `availableSizes` (Array): 可选尺码列表
- `yarnIds` (Array): 关联的纱线ID列表
- `remark` (String): 备注
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记（默认 false）

**必要索引**:
- `idx_deleted`: `deleted` (普通索引) - 用于过滤已删除的款号记录（必须创建）
- `idx_tenantId`: `tenantId` (普通索引) - 用于查询租户下的款号
- `idx_styleCode`: `styleCode` (普通索引) - 用于根据款号编号查询
- `idx_deleted_createTime`: `deleted` (asc) + `createTime` (desc) (复合索引) - 用于款号列表查询（按创建时间倒序）

---

### 4. factories（加工厂表）
**用途**: 存储加工厂信息

**权限设置**: 
- **推荐**: `仅创建者可读写`
- **说明**: 加工厂信息应通过云函数统一管理，前端通过云函数接口访问

**字段结构**:
- `tenantId` (String): 租户ID
- `name` (String): 加工厂名称
- `contact` (String): 联系人
- `phone` (String): 联系方式
- `defaultPrice` (Number): 默认加工单价（元/打）
- `settlementMethod` (String): 结算方式（次结/月结）
- `remark` (String): 备注
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记（默认 false）

**必要索引**:
- `idx_deleted`: `deleted` (普通索引) - 用于过滤已删除的加工厂记录（必须创建）
- `idx_tenantId`: `tenantId` (普通索引) - 用于查询租户下的加工厂
- `idx_name`: `name` (普通索引) - 用于根据名称查询加工厂

---

### 5. yarn_inventory（纱线库存表）
**用途**: 存储纱线库存信息

**权限设置**: 
- **推荐**: `仅创建者可读写`
- **说明**: 库存数据敏感，应通过云函数统一管理，前端通过云函数接口访问

**字段结构**:
- `tenantId` (String): 租户ID
- `yarnName` (String): 纱线名称/批次
- `color` (String): 颜色
- `currentStock` (Number): 当前库存数量（kg）
- `remark` (String): 备注
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记（默认 false）

**必要索引**:
- `idx_deleted`: `deleted` (普通索引) - 用于过滤已删除的纱线库存记录
- `idx_tenantId`: `tenantId` (普通索引) - 用于查询租户下的纱线库存
- `idx_yarnName`: `yarnName` (普通索引) - 用于根据纱线名称查询
- `idx_deleted_createTime`: `deleted` (asc) + `createTime` (desc) (复合索引) - 用于纱线列表查询（按创建时间倒序）

---

### 6. issue_orders（发料单表）
**用途**: 存储发料单信息

**权限设置**: 
- **推荐**: `仅创建者可读写`
- **说明**: 发料单数据包含业务核心信息，应通过云函数统一管理，前端通过云函数接口访问

**字段结构**:
- `tenantId` (String): 租户ID
- `issueNo` (String): 发料单号
- `factoryId` (String): 加工厂ID
- `styleId` (String): 款号ID
- `color` (String): 颜色
- `size` (String): 尺码
- `issueWeight` (Number): 发料重量（kg）
- `issueDate` (Date): 发料日期
- `planId` (String): 关联生产计划单ID
- `status` (String): 状态（未回货/部分回货/已回货）
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记（默认 false）

**必要索引**:
- `idx_deleted`: `deleted` (普通索引) - 用于过滤已删除的发料单记录（必须创建）
- `idx_issueNo`: `issueNo` (普通索引) - 用于根据发料单号查询
- `idx_factoryId`: `factoryId` (普通索引) - 用于根据加工厂ID查询
- `idx_styleId`: `styleId` (普通索引) - 用于根据款号ID查询
- `idx_issueDate`: `issueDate` (普通索引) - 用于根据日期查询
- `idx_deleted_issueDate`: `deleted` (asc) + `issueDate` (desc) (复合索引) - ⚠️ **必须创建** - 用于发料单列表查询（按日期倒序）

**可选索引**（根据业务需要）:
- `idx_factoryId_deleted_issueDate`: `factoryId` (asc) + `deleted` (asc) + `issueDate` (desc) (复合索引) - 用于加工厂详情页查询发料单

---

### 7. return_orders（回货单表）
**用途**: 存储回货单信息

**权限设置**: 
- **推荐**: `仅创建者可读写`
- **说明**: 回货单数据包含业务核心信息，应通过云函数统一管理，前端通过云函数接口访问

**字段结构**:
- `tenantId` (String): 租户ID
- `returnNo` (String): 回货单号
- `factoryId` (String): 加工厂ID
- `issueId` (String): 发料单ID
- `styleId` (String): 款号ID
- `returnQuantity` (Number): 回货数量（打）
- `returnPieces` (Number): 回货件数
- `actualYarnUsage` (Number): 实际用纱量（kg）
- `returnDate` (Date): 回货日期
- `processingFee` (Number): 加工费
- `settledAmount` (Number): 已结算金额
- `settlementStatus` (String): 结算状态（未结算/部分结算/已结算）
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记（默认 false）

**必要索引**:
- `idx_deleted`: `deleted` (普通索引) - 用于过滤已删除的回货单记录（必须创建）
- `idx_returnNo`: `returnNo` (普通索引) - 用于根据回货单号查询
- `idx_factoryId`: `factoryId` (普通索引) - 用于根据加工厂ID查询
- `idx_issueId`: `issueId` (普通索引) - 用于根据发料单ID查询
- `idx_returnDate`: `returnDate` (普通索引) - 用于根据日期查询
- `idx_settlementStatus`: `settlementStatus` (普通索引) - 用于根据结算状态查询
- `idx_deleted_issueId`: `deleted` (asc) + `issueId` (asc) (复合索引) - ⚠️ **最高优先级，必须创建** - 用于批量查询多个发料单的回货单（避免N+1查询）
- `idx_deleted_returnDate`: `deleted` (asc) + `returnDate` (desc) (复合索引) - ⚠️ **必须创建** - 用于回货单列表查询（按日期倒序）
- `idx_deleted_settlementStatus`: `deleted` (asc) + `settlementStatus` (asc) (复合索引) - 用于查询未结算回货单（首页统计）

**可选索引**（根据业务需要）:
- `idx_factoryId_deleted_returnDate`: `factoryId` (asc) + `deleted` (asc) + `returnDate` (desc) (复合索引) - 用于加工厂详情页查询回货单

---

### 8. color_dict（颜色字典表）
**用途**: 存储颜色字典数据

**权限设置**: 
- **推荐**: `所有用户可读，仅创建者可写` 或 `仅创建者可读写`
- **说明**: 字典数据如果前端需要直接读取（用于下拉选择等），可设置为"所有用户可读"；如果仅通过云函数访问，设置为"仅创建者可读写"

**字段结构**:
- `tenantId` (String): 租户ID
- `name` (String): 颜色名称
- `code` (String): 颜色编码
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间

**必要索引**:
- `idx_tenantId`: `tenantId` (普通索引) - 用于查询租户下的颜色
- `idx_name`: `name` (普通索引) - 用于根据名称查询
- `idx_tenantId_name`: `tenantId` (asc) + `name` (asc) (唯一复合索引) - 用于防止同一租户下重复的颜色名称

---

### 9. size_dict（尺码字典表）
**用途**: 存储尺码字典数据

**权限设置**: 
- **推荐**: `所有用户可读，仅创建者可写` 或 `仅创建者可读写`
- **说明**: 字典数据如果前端需要直接读取（用于下拉选择等），可设置为"所有用户可读"；如果仅通过云函数访问，设置为"仅创建者可读写"

**字段结构**:
- `tenantId` (String): 租户ID
- `name` (String): 尺码名称
- `code` (String): 尺码编码
- `order` (Number): 排序序号
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间

**必要索引**:
- `idx_tenantId`: `tenantId` (普通索引) - 用于查询租户下的尺码
- `idx_name`: `name` (普通索引) - 用于根据名称查询
- `idx_tenantId_name`: `tenantId` (asc) + `name` (asc) (唯一复合索引) - 用于防止同一租户下重复的尺码名称
- `idx_tenantId_order_createTime`: `tenantId` (asc) + `order` (asc) + `createTime` (desc) (复合索引) - 用于尺码列表查询（按排序字段和创建时间）

---

### 10. production_plans（生产计划单表）
**用途**: 存储生产计划信息

**权限设置**: 
- **推荐**: `仅创建者可读写`
- **说明**: 生产计划包含核心业务进度，应由云函数统一管理

**字段结构**:
- `tenantId` (String): 租户ID
- `planNo` (String): 计划单号
- `styleId` (String): 款号ID
- `totalQuantity` (Number): 计划总量
- `status` (String): 状态
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记

**必要索引**:
- `idx_deleted`: `deleted` (普通索引)
- `idx_tenantId_createTime`: `tenantId` (asc) + `createTime` (desc)

---

### 11. settlements（结算单表）
**用途**: 存储结算记录

**权限设置**: 
- **推荐**: `仅创建者可读写`

**字段结构**:
- `tenantId` (String): 租户ID
- `settlementNo` (String): 结算单号
- `factoryId` (String): 加工厂ID
- `factoryName` (String): 工厂名称
- `settlementDate` (Date): 结算日期
- `totalAmount` (Number): 结算总金额
- `remark` (String): 备注
- `returnOrderIds` (Array): 关联的回货单ID列表
- `createTime` (Date): 创建时间
- `updateTime` (Date): 更新时间
- `deleted` (Boolean): 软删除标记

**必要索引**:
- `idx_deleted`: `deleted` (普通索引)
- `idx_factoryId`: `factoryId` (普通索引)
- `idx_settlementDate`: `settlementDate` (降序)

---

## 🚨 索引创建优先级

### ⚠️ 最高优先级（立即创建 - 解决性能问题）

1. **`return_orders`: idx_deleted_issueId**
   - 字段：`deleted` (asc) + `issueId` (asc)
   - 用途：批量查询多个发料单的回货单
   - 快速创建链接：
     ```
     cloud://createindex?env=cloud1-1gzk1uq14c3065cb&collection=return_orders&from=console&s=%5B%7B%22field%22%3A%22deleted%22%2C%22type%22%3A1%7D%2C%7B%22field%22%3A%22issueId%22%2C%22type%22%3A1%7D%5D
     ```

2. **`issue_orders`: idx_deleted_issueDate**
   - 字段：`deleted` (asc) + `issueDate` (desc)
   - 用途：发料单列表查询（按日期倒序）
   - 快速创建链接：
     ```
     cloud://createindex?env=cloud1-1gzk1uq14c3065cb&collection=issue_orders&from=console&s=%5B%7B%22field%22%3A%22deleted%22%2C%22type%22%3A1%7D%2C%7B%22field%22%3A%22issueDate%22%2C%22type%22%3A-1%7D%5D
     ```

3. **`return_orders`: idx_deleted_returnDate**
   - 字段：`deleted` (asc) + `returnDate` (desc)
   - 用途：回货单列表查询（按日期倒序）
   - 快速创建链接：
     ```
     cloud://createindex?env=cloud1-1gzk1uq14c3065cb&collection=return_orders&from=console&s=%5B%7B%22field%22%3A%22deleted%22%2C%22type%22%3A1%7D%2C%7B%22field%22%3A%22returnDate%22%2C%22type%22%3A-1%7D%5D
     ```

4. **`styles`: idx_deleted**
   - 字段：`deleted` (asc)
   - 用途：款号列表 count 查询
   - 快速创建链接：
     ```
     cloud://createindex?env=cloud1-1gzk1uq14c3065cb&collection=styles&from=console&s=%5B%7B%22field%22%3A%22deleted%22%2C%22type%22%3A1%7D%5D
     ```

5. **`factories`: idx_deleted**
   - 字段：`deleted` (asc)
   - 用途：加工厂列表 count 查询
   - 快速创建链接：
     ```
     cloud://createindex?env=cloud1-1gzk1uq14c3065cb&collection=factories&from=console&s=%5B%7B%22field%22%3A%22deleted%22%2C%22type%22%3A1%7D%5D
     ```

6. **`issue_orders`: idx_deleted**
   - 字段：`deleted` (asc)
   - 用途：发料单列表查询（不带排序）
   - 快速创建链接：
     ```
     cloud://createindex?env=cloud1-1gzk1uq14c3065cb&collection=issue_orders&from=console&s=%5B%7B%22field%22%3A%22deleted%22%2C%22type%22%3A1%7D%5D
     ```

### 📌 第二批（尽快创建）

7. **`return_orders`: idx_deleted_settlementStatus**
   - 字段：`deleted` (asc) + `settlementStatus` (asc)
   - 用途：查询未结算回货单
   - 快速创建链接：
     ```
     cloud://createindex?env=cloud1-1gzk1uq14c3065cb&collection=return_orders&from=console&s=%5B%7B%22field%22%3A%22deleted%22%2C%22type%22%3A1%7D%2C%7B%22field%22%3A%22settlementStatus%22%2C%22type%22%3A1%7D%5D
     ```

### 📝 第三批（可选 - 进一步优化）

8. **`issue_orders`: idx_factory_deleted_issueDate**
   - 字段：`factoryId` (asc) + `deleted` (asc) + `issueDate` (desc)
   - 用途：加工厂详情页查询

9. **`return_orders`: idx_factory_deleted_returnDate**
   - 字段：`factoryId` (asc) + `deleted` (asc) + `returnDate` (desc)
   - 用途：加工厂详情页查询

10. **`styles`: idx_deleted_createTime**
    - 字段：`deleted` (asc) + `createTime` (desc)
    - 用途：款号列表查询（按创建时间倒序）

11. **`yarn_inventory`: idx_deleted_createTime**
    - 字段：`deleted` (asc) + `createTime` (desc)
    - 用途：纱线列表查询

12. **`color_dict`: idx_tenantId_name**
    - 字段：`tenantId` (asc) + `name` (asc)
    - 用途：唯一索引，防止重复

13. **`size_dict`: idx_tenantId_name**
    - 字段：`tenantId` (asc) + `name` (asc)
    - 用途：唯一索引，防止重复

14. **`size_dict`: idx_tenantId_order_createTime**
    - 字段：`tenantId` (asc) + `order` (asc) + `createTime` (desc)
    - 用途：尺码列表查询（按排序字段）

---

## 📝 创建步骤

### 方法一：通过云开发控制台创建（推荐）

1. 打开微信开发者工具
2. 点击顶部菜单栏的"云开发"
3. 进入"数据库"标签页
4. 选择对应的集合（如 `return_orders`）
5. 点击"索引管理"标签
6. 点击"添加索引"
7. 输入索引字段：
   - 第一个字段：`deleted`，排序：升序
   - 第二个字段：`issueId`，排序：升序
8. 点击"确定"创建
9. 等待索引创建完成（可能需要几分钟）

### 方法二：使用快速创建链接

在微信开发者工具的控制台（Console）中，直接点击日志中显示的快速创建链接，或者在浏览器地址栏中输入完整的链接。

**链接格式**:
```
cloud://createindex?env=环境ID&collection=集合名&from=console&s=索引字段JSON
```

---

## ✅ 创建检查清单

### ⚠️ 最高优先级（立即创建）
- [ ] `return_orders`: idx_deleted_issueId
- [ ] `issue_orders`: idx_deleted_issueDate
- [ ] `return_orders`: idx_deleted_returnDate
- [ ] `styles`: idx_deleted
- [ ] `factories`: idx_deleted
- [ ] `issue_orders`: idx_deleted

### 📌 第二批（尽快创建）
- [ ] `return_orders`: idx_deleted_settlementStatus

### 📝 第三批（可选）
- [ ] `issue_orders`: idx_factory_deleted_issueDate
- [ ] `return_orders`: idx_factory_deleted_returnDate
- [ ] `styles`: idx_deleted_createTime
- [ ] `yarn_inventory`: idx_deleted_createTime
- [ ] `color_dict`: idx_tenantId_name
- [ ] `size_dict`: idx_tenantId_name
- [ ] `size_dict`: idx_tenantId_order_createTime

---

## ⚠️ 注意事项

1. **索引数量限制**: 每个集合最多可创建 20 个索引
2. **索引存储**: 索引会占用额外的存储空间（通常很小）
3. **写入性能**: 索引会略微影响写入性能（通常可以忽略）
4. **复合索引顺序**: 复合索引的字段顺序很重要，必须按照查询条件顺序创建
5. **字段命名**: 云数据库使用驼峰命名（如 `issueDate`），不是下划线命名（如 `issue_date`）

---

---

## 📋 权限设置检查清单

- [ ] `tenants`: 权限设置为"仅创建者可读写" 或 "所有用户不可读写"
- [ ] `users`: 权限设置为"所有用户不可读写"
- [ ] `styles`: 权限设置为"仅创建者可读写"
- [ ] `factories`: 权限设置为"仅创建者可读写"
- [ ] `yarn_inventory`: 权限设置为"仅创建者可读写"
- [ ] `issue_orders`: 权限设置为"仅创建者可读写"
- [ ] `return_orders`: 权限设置为"仅创建者可读写"
- [ ] `color_dict`: 权限设置为"所有用户可读，仅创建者可写"
- [ ] `size_dict`: 权限设置为"所有用户可读，仅创建者可写"

---

**最后更新**: 2025-01-XX
**云环境ID**: cloud1-1gzk1uq14c3065cb

